{
  "name": "timeforzeros-website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "react": "cd client && npm start",
    "express": "cd server && nodemon",
    "dev": "concurrently \"cd client && npm start\" \"cd server && nodemon\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0",
    "config": "^3.3.2"
  }
}
